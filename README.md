# samthambad/homebrew-tap

Homebrew tap for **Naviin** — a terminal-native financial platform for technical individuals.

## Install

```bash
brew install samthambad/tap/naviin
```

## Update

```bash
brew upgrade naviin
```

Releases are published automatically when a `v*` tag is pushed to the main repo.
